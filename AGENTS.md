1. 除非用户明确要求重构、优化或修改全局样式、JS以及组件,否则组件仅创建文件
2. 尽量使用已有样式
3. 来自哔哩哔哩的图片、视频和音频必须设置无referrer
4. 底层需API的功能默认支持json文件和请求API
5. (补充4)当有json文件但为空时,仅写页面,禁止猜测API返回,需API驱动的部分可以暂时mock,但当json内容不为空时,必须从JSON获取数据
6. 当用户要求复刻xx项目的xx文件时,除非用户要求保留,否则去除一些与作者强相关的内容,如联系方式、出现人名/用户名的元素
7. 需强交互的组件用svelte,否则使用astro
8. 忽略.obsdian的所有更改
9. 默认组件必须实现国际化,可修改i18n下的文件
10. 使用CSS变量时必须清楚变量类型